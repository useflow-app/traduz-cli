name: automerge
on:
  workflow_dispatch:
  schedule:
    - cron: '17 2,5,8,11 * * *'

jobs:
  automerge:
    uses: oclif/github-workflows.old/.github/workflows.old/automerge.yml@main
    secrets: inherit
